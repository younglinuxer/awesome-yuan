input {
  beats {
    port => 5044
    #设置编码
    codec => json {charset => "UTF-8"}
     #当存在多个文件的时候可使用type指定输入输出路径
    }
}

#2.过滤格式化数据阶段
filter {
}
#3.数据输出到ES阶段
output {
	elasticsearch {
                  hosts => ["172.16.12.88:9200"]
                  #索引
                  index => "nginx-8070-%{yyyyMMdd}"
                  #类型
                  codec => json {charset => "UTF-8"}

          }

}

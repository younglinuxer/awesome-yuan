version: '3.8'

services:
  xxl-job-admin:
    image: xuxueli/xxl-job-admin:2.4.1
    container_name: xxl-job-admin
    restart: always
    environment:
      - PARAMS=--spring.datasource.url=jdbc:mysql://10.100.17.34:3306/xxl_job?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai \
              --spring.datasource.username=root \
              --spring.datasource.password=CTW$3u^7gfUh \
              --xxl.job.accessToken=xQJwgQkXwZpq
    ports:
      - "8088:8080"
    volumes:
      - /data/xxljob/logs:/data/applogs
